# FaultScan Daily Log — 2026-08-02

## Work completed

- Reviewed the current repository state, recent commit and reflog history,
  `TASKS.md`, and the active `rp_input.json` configuration while preparing
  this log.
- No commits or reflog entries dated August 2 were present in the inspected
  local history. `HEAD`, `main`, and `origin/main` remain at `f846e51`
  (`Record July project updates`).
- The working tree contains pre-existing uncommitted changes to `README.md`,
  `align_stack.py`, `plot_event_rt_snippets.py`, `rp_input.json`,
  `tests/test_align_stack_smoke.py`, and
  `tests/test_plot_event_rt_snippets.py`, plus local `.DS_Store` metadata and
  untracked daily notes for July 31 and August 1. They were preserved.
- The pending implementation diff changes tabulated-statics defaults to the
  `sta_statics_R`/`sta_statics_T` workbook-column convention; restricts
  application of stored statics to S alignment; retains P/S
  cross-correlation residual measurement; accepts the legacy `tabulate_T`
  spelling; and adds associated smoke-test coverage. This is uncommitted
  implementation state, not evidence of execution or a scientific result.
- This automation created only this daily log. It did not modify programs,
  configuration, research inputs, or scientific outputs.

## Verification

- No tests were run during this log update. The pending smoke-test additions
  are present but unexecuted; the historical test checkpoints recorded in
  earlier repository notes were not rerun.
- No scientific processing was run, and no new scientific outputs or plots
  were generated or reviewed today.
- `git diff --check` reported no whitespace errors before or after this log
  was added.

## Current active configuration

- `rp_input.json` selects 250 Hz snippets, all channels, T/S alignment, a
  10--25 Hz filter, and a 4.5--6.5 s interval. The configured event list has
  27 events; `CI_40353472` remains the event-alignment reference with a 0.2 s
  maximum event-stack shift.
- Station-static mode is currently `tabulated`, using the external
  `stations.xlsx` workbook and its `sta_statics_T` column. Event-static
  correction and JSON event-location override are enabled. These are
  uncommitted configuration settings and do not demonstrate that a run
  occurred.
- The current screening values include `r_window_min: 0.5`, a 0.05 s move
  limit, and a 10.0 peak-to-pre-P-median threshold. Individual seismograms,
  original overlays, and record-section plotting are enabled.

## Scientific observations and interpretation

- No new pipeline-derived scientific conclusion was established today.
- FS-003, FS-004, and FS-005 remain open in `TASKS.md`; their stated
  representative validation criteria are not demonstrated by this repository
  review. In particular, the pending tabulated configuration is not evidence
  of the matched comparisons required by FS-004.

## Unresolved questions and next steps

- Review and test the pending implementation/configuration changes before
  relying on them; testing would verify implementation only, not scientific
  validation.
- Validate FS-003 and FS-004 with their stated representative matched
  component/frequency comparisons before interpreting station-static effects.
- Validate P-wave statics for FS-005 without overwriting the established
  S-static columns.
