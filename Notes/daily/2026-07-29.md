# FaultScan Daily Log — 2026-07-29

## Work completed

- Reviewed the current repository state, confirmed commit activity, `TASKS.md`,
  and the active `rp_input.json` configuration while preparing this log.
- No commits dated July 29 were present at review time. The most recent
  confirmed repository activity remains the July 28 commits `d8252b6`
  (component-diagnostic final-selection coloring), `9c0b375` (plotting and
  stack-tool alignment with then-active configuration), and `c3c20b0`
  (project-record refresh).
- Before this log was created, the working tree already contained unrelated
  modifications to `.DS_Store`, `Notes/daily/2026-07-28.md`, and
  `rp_input.json`. They were preserved. The `rp_input.json` change is local
  and uncommitted; it is not evidence of a completed processing run.
- This automation updated only this daily log. It did not modify programs,
  configuration, research inputs, or scientific outputs.

## Verification

- No tests were run during this log update. No current-day test-execution
  record was found.
- No scientific processing was run, and no new scientific outputs or plots
  were generated or reviewed during this update.

## Current active configuration

- Input is 250 Hz snippets with all channels enabled, P alignment, 10--25 Hz
  filtering, and a -2--12 s analysis interval. The separately configured
  component is `T`.
- Event-static correction is disabled, so catalog `time shift` values are not
  applied. `CI_40353472` is the event-alignment reference, with a 0.2 s
  maximum event-stack alignment shift.
- Station-static mode is `none`; the configured external station workbook and
  `station static s` column are retained but not applied. The maximum station
  move is 0.05 s, the correlation threshold is `r_window_min: -1`, and the
  pre-P noise-screen threshold is `trace_peak_to_pre_p_median_min: 3.0`.
- JSON event-location override is enabled at 33.480 N, 116.513 W, and 9.300
  km. Individual seismograms, original overlays, and record-section plotting
  are enabled.

## Scientific observations and interpretation

- No new pipeline-derived scientific conclusion was established today.
- FS-003, FS-004, and FS-005 remain unchecked in `TASKS.md`. Their stated
  representative validation criteria have not been demonstrated by this
  repository review.

## Unresolved questions and next steps

- Run an appropriate test set before relying on recent implementation changes.
- Validate FS-003 and FS-004 with matched `none`, `tabulated`, and
  `cross_correlation` runs before interpreting station-static effects.
- Validate P-wave statics for FS-005 without overwriting the established
  S-static column.
