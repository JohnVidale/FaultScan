# FaultScan Daily Log — 2026-07-27

## Work completed

- Reviewed the repository state, recent commit and reflog activity, `TASKS.md`,
  the active `rp_input.json` configuration, `README.md`, project status, and
  the durable project chat summary while preparing this log.
- Fetched the configured `origin` GitHub remote. At review time, local `main`
  and `origin/main` both pointed to `997d177` (`Record documentation sync in
  daily log`); no remote-only commits were found.
- The pending worktree contains changes to alignment and utility code, the
  stacker and R/T snippet plotter, tests, README, project status, and active
  configuration, plus an untracked `tests/test_plot_event_rt_snippets.py` and
  local `.DS_Store` metadata. These are implementation and documentation
  state; no task is considered complete from their presence.
- This documentation update modified only this daily log and the durable chat
  summary; it did not modify programs, configuration, research inputs, or
  scientific outputs.

## Verification

- No tests were run during this update. The most recently recorded focused
  45-test checkpoint and 58-test full-discovery checkpoint are historical
  implementation evidence; their transcripts were not rerun or independently
  reviewed today and do not validate the present pending worktree.
- No scientific processing was run, and no new scientific output was reviewed
  or produced today.

## Current active configuration

- Input is 250 Hz snippets with all channels enabled, S alignment, 1--4 Hz
  filtering, and a -2--12 s analysis interval. The separate configured
  component is `T`.
- Event-static correction is enabled using the shared catalog `time shift`
  column. `CI_40353472` remains the event-alignment reference and the maximum
  event-stack alignment shift is 0.2 s.
- Station-static mode is `tabulated`, using the configured external
  `stations.xlsx` file and `station static s` column. The correlation threshold
  is 0.2, the maximum station move is 0.05 s, and the pre-P noise screen is
  configured at `trace_peak_to_pre_p_median_min: 20.0`.
- JSON event-location override is enabled at 33.480 N, 116.513 W, and 9.300
  km. Individual seismograms, original overlays, and record-section plotting
  are enabled.

## Scientific observations and interpretation

- No new pipeline-derived scientific conclusion was established today.
- FS-003, FS-004, and FS-005 remain unchecked in `TASKS.md`; their required
  representative multi-event, component, and frequency validation has not
  been demonstrated by this documentation review.

## Unresolved questions and next steps

- Run appropriate tests before relying on the pending source and test changes.
- Validate FS-003 and FS-004 with matched `none`, `tabulated`, and
  `cross_correlation` runs before interpreting any station-static choice.
- Validate P-wave statics for FS-005 without changing the established S-static
  column.
