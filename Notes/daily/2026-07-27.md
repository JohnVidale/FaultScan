# FaultScan Daily Log — 2026-07-27

## Work completed

- Reviewed the current repository state, confirmed July 27 commits and reflog
  activity, `TASKS.md`, the active `rp_input.json` configuration, `README.md`,
  project status, and science goals while preparing this log.
- Confirmed six commits on `main` between 18:48 and 18:56 PDT:
  `23db1d3` added the July 27 log and project chat summary; `73238a0`
  clarified the FS-003--FS-005 validation criteria; `21f6a1b` added
  screening metrics and associated alignment/configuration/test changes;
  `5764f99` updated event R/T snippet plotting and added its test module;
  `4499403` changed stack comparison output handling and tests; and
  `12f2adc` documented the shared stack-output workflow.
- At review time, `HEAD`, `main`, and `origin/main` were all `12f2adc`
  (`Document shared stack output workflow`). The only uncommitted change was
  local `.DS_Store` metadata; source, configuration, tests, task queue, and
  documentation changes described above are committed.
- This automation updated only this daily log. It did not modify programs,
  configuration, research inputs, or scientific outputs.

## Verification

- No tests were run during this log update. The commits include test changes,
  but no current-day test execution transcript was reviewed; they are not
  evidence that the committed implementation is validated.
- No scientific processing was run, and no scientific outputs were generated
  or reviewed during this update.

## Current active configuration

- Input is 250 Hz snippets with all channels enabled, S alignment, 1--4 Hz
  filtering, and a -2--12 s analysis interval. The separate configured
  component is `T`.
- Event-static correction is enabled using the shared catalog `time shift`
  column. `CI_40353472` is the event-alignment reference and the maximum
  event-stack alignment shift is 0.2 s.
- Station-static mode is `tabulated`, using the configured external
  `stations.xlsx` file and `station static s` column. The correlation threshold
  is 0.2, the maximum station move is 0.05 s, and the pre-P noise screen is
  configured at `trace_peak_to_pre_p_median_min: 20.0`.
- JSON event-location override is enabled at 33.480 N, 116.513 W, and 9.300
  km. Individual seismograms, original overlays, and record-section plotting
  are enabled.

## Scientific observations and interpretation

- No new pipeline-derived scientific conclusion was established today.
- FS-003, FS-004, and FS-005 remain unchecked in `TASKS.md`. The committed
  implementation, configuration, and test changes do not satisfy their
  required representative multi-event, component, and frequency validation.

## Unresolved questions and next steps

- Run an appropriate test set before relying on the committed implementation
  changes.
- Validate FS-003 and FS-004 with matched `none`, `tabulated`, and
  `cross_correlation` runs before interpreting any station-static choice.
- Validate P-wave statics for FS-005 without changing the established S-static
  column.
