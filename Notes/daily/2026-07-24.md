# FaultScan Daily Log — 2026-07-24

## Work completed

- Reviewed the repository state, recent repository activity, `TASKS.md`, the
  active `rp_input.json` configuration, and the untracked static-comparison
  workbook while preparing this log.
- No commits or branch reflog entries are dated today. `main` and
  `origin/main` remain at `fdf9001` (`Add alignment tools and project status
  notes`, 2026-07-22).
- Files with 2026-07-24 modification times include uncommitted alignment and
  stacking code, tests, `rp_input.json`, project notes, `README.md`, and
  `TASKS.md`. Their timestamps confirm same-day repository activity, but not
  completion or validation of the work they contain.
- A same-day untracked workbook,
  `outputs/static_comparison_20260724/CI_40353552_static_shift_comparison_3-10Hz.xlsx`,
  and its inspection record were created at 09:20 PDT. The workbook compares
  stored and correlation-estimated S station statics for event `CI_40353552`
  at 3--10 Hz; its embedded provenance says the source residuals were
  generated on 2026-07-22 and the referenced timestamped run directory is no
  longer present. It is therefore evidence of a 2026-07-24 comparison report,
  not evidence of a fresh pipeline run today.
- The current uncommitted working tree includes a three-mode station-static
  control in `align_stack.py`: `none`, `tabulated`, and
  `cross_correlation`. The active configuration selects `tabulated`.
- Current uncommitted alignment changes make the final stack use traces that
  passed correlation screening rather than all aligned traces. This is
  implementation state only; no completed scientific validation is
  established by the change.
- This documentation update did not modify programs or configuration,
  implement a task, run tests, or run the scientific processing pipeline.

## Verification

- No tests were run during this log update. The repository contains no
  dated test transcript or other evidence that verifies the current
  uncommitted working tree today.
- No scientific processing was run during this log update. The present
  same-day workbook is a post hoc comparison artifact whose stated source
  residuals are from 2026-07-22, not a new processing result.
- The most recent recorded full-suite result remains **40 passing tests** from
  the 2026-07-22 daily log. It is historical verification and does not verify
  the current uncommitted changes.

## Current active configuration

- Input is 250 Hz snippets, all channels (`all_channels: true`), S alignment,
  1--10 Hz filtering, and a -2--12 s analysis interval. The separate
  configured component is `T`.
- Event-static correction is enabled, using the shared catalog `time shift`
  column. The event reference is `CI_40353472`, and the event-stack alignment
  limit is 0.2 s.
- Station-static mode is `tabulated`, using `station static s` from the
  configured external `stations.xlsx` file. In this mode the current code
  imposes table values rather than calculating and writing a new
  cross-correlation station-statics workbook.
- The correlation-window padding is 0.05 s before and 0.15 s after; maximum
  station move is 0.05 s. JSON event-location override remains enabled at
  33.480 N, 116.513 W, and 9.300 km.
- Individual-seismogram, original-overlay, and record-section plotting are
  all disabled in the active JSON configuration.

## Outputs and results reviewed

- The static-comparison workbook reports 278 matched stations for each
  component and 251, 278, and 274 traces passing its correlation screen for
  Z, R, and T respectively. Its reported stored-versus-estimated correlations
  are 0.315 (Z), 0.962 (R), and -0.064 (T); the report interprets this as
  close agreement only for R. These reported values concern the historical
  residual inputs described above and have not been independently rerun today.
- Four tracked station-statics PNG assets remain deleted in the working tree:
  R-S and T-S plots and their event-baseline-corrected counterparts. Their
  intended disposition remains unresolved.
- Uncommitted changes are present in source, tests, configuration, notes,
  tools, and assets. They were preserved without alteration by this log
  update.

## Scientific observations and interpretation

- No new pipeline-derived scientific conclusion was established today.
- The comparison workbook supports a limited, report-scoped observation: the
  stored S correction agrees closely with the radial correlation pattern for
  its one evaluated event, but not with Z or T. Its own provenance cautions
  that the stations workbook does not record the correction's derivation; it
  does not complete the broader validation tasks.
- FS-003, FS-004, and FS-005 remain unchecked in `TASKS.md`. In particular,
  the new station-static modes and changed stack-selection behavior have not
  been demonstrated across representative events, components, frequencies,
  and station counts.

## Decisions

- Continue to distinguish same-day artifacts and repository implementation
  state from rerun, validated scientific results.
- Preserve historical test evidence separately from any test run that would
  validate the current working tree.

## Unresolved questions and next steps

- FS-003: verify static-shift coding and application across representative
  events, components, frequencies, and station counts.
- FS-004: compare matched `none`, `tabulated`, and `cross_correlation`
  station-static runs and summarize alignment, correlation, and coherence.
- FS-005: establish and validate P-wave station shifts without overwriting the
  existing S-statics column.
- Decide whether the four deleted station-statics assets should be restored,
  regenerated, or deliberately removed before a commit.
