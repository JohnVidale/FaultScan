# FaultScan Daily Log — 2026-07-25

## Work completed

- Reviewed the repository state, today’s commit and reflog activity,
  `TASKS.md`, and the active `rp_input.json` configuration while preparing
  this log.
- Committed `4348661` at 19:31 PDT: `Add static alignment modes and stacker
  workflow`. It adds the recent daily logs, task queue, project notes,
  README, static-comparison workbook and inspection record, source and test
  changes, and the catalog-shift update tool. It also records deletion of the
  four station-statics PNG assets previously noted as missing.
- The working tree after that commit has uncommitted changes in
  `align_stack.py`, `align_utils.py`, `rp_input.json`, and
  `tests/test_align_utils_unit.py` only. They add a configurable minimum ratio
  of whole-trace peak amplitude to median absolute pre-P amplitude, apply it
  alongside the existing correlation screen, report separate rejection counts,
  and add unit coverage for the ratio and rejection accounting.
- The active configuration sets that new
  `trace_peak_to_pre_p_median_min` threshold to `10.0`. This is current
  implementation/configuration state, not validation that it improves the
  selected traces or stacks.
- A 22:00 PDT follow-up review found no newer commits or reflog entries, no
  additional pending files, and no new processing outputs beyond an
  `outputs/.DS_Store` metadata file. The repository evidence therefore remains
  as described above.
- This documentation update did not modify programs or configuration,
  implement a task, run tests, or run the scientific processing pipeline.

## Verification

- No tests were run during this log update. No same-day repository test
  transcript or result was found that verifies the commit or the current
  uncommitted changes.
- No scientific processing was run during this log update, and no new
  pipeline output dated today was found. The committed static-comparison
  workbook remains the 2026-07-24 report whose embedded provenance identifies
  2026-07-22 residual inputs.
- The most recent recorded full-suite result remains **40 passing tests** from
  2026-07-22. It is historical verification and does not validate the 2026-07-25
  commit or pending changes.

## Current active configuration

- Input is 250 Hz snippets, all channels (`all_channels: true`), S alignment,
  1--10 Hz filtering, and a -2--12 s analysis interval. The separate
  configured component is `T`.
- Event-static correction is enabled, using the shared catalog `time shift`
  column. The event reference is `CI_40353472`, and the event-stack alignment
  limit is 0.2 s.
- Station-static mode is `tabulated`, using `station static s` from the
  configured external `stations.xlsx` file. The correlation-window padding is
  0.05 s before and 0.15 s after, the correlation threshold is 0.6, and the
  maximum station move is 0.05 s.
- JSON event-location override remains enabled at 33.480 N, 116.513 W, and
  9.300 km. Individual-seismogram, original-overlay, and record-section
  plotting are disabled.

## Outputs and results reviewed

- The repository now tracks the `CI_40353552` stored-versus-estimated
  station-statics comparison workbook and its inspection record. Its reported
  R correlation is 0.962, whereas Z and T are 0.315 and -0.064 respectively;
  those figures are limited to its historical residual inputs and one event.
- The commit contains the four deleted R-S/T-S station-statics image assets.
  The commit establishes that the deletions are now versioned; it does not
  establish whether replacements should later be generated.

## Scientific observations and interpretation

- No new pipeline-derived scientific conclusion was established today.
- FS-003, FS-004, and FS-005 remain unchecked in `TASKS.md`. The report for
  one event and the new screening implementation do not satisfy their required
  multi-event/component/frequency validation.

## Decisions

- Keep the new amplitude-ratio screen separate from evidence that it produces
  a better scientific selection until it is tested on representative runs.
- Continue to distinguish committed implementation state, pending changes,
  and historical results from same-day verified processing.

## Unresolved questions and next steps

- FS-003: verify static-shift coding and application across representative
  events, components, frequencies, and station counts.
- FS-004: compare matched `none`, `tabulated`, and `cross_correlation`
  station-static runs and summarize alignment, correlation, and coherence.
- FS-005: establish and validate P-wave station shifts without overwriting the
  existing S-statics column.
- Run an appropriate test set and representative processing only when desired
  to validate the committed and pending screening changes.
