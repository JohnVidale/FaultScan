# FaultScan Daily Log — 2026-07-26

## Work completed

- Reviewed the repository state, confirmed commit and reflog activity,
  `TASKS.md`, the active `rp_input.json` configuration, and existing project
  documentation while preparing this log.
- Committed and pushed `e60e73b` at 10:29 PDT: `Add July 25 and 26 daily
  logs`. It adds the two daily Markdown files only; it does not include the
  current uncommitted source, configuration, test, or documentation changes.
- Committed and pushed `895bc6d`: `Add FaultScan agent guidance and chat
  updates`. It adds the root `AGENTS.md` and updates this daily log and the
  project chat summary; it does not include the remaining source,
  configuration, test, README, or project-status changes.
- The current uncommitted worktree includes updates to alignment and utility
  code, the stacker and event R/T snippet plotter, tests, README and project
  status note, and `rp_input.json`, plus an untracked
  `tests/test_plot_event_rt_snippets.py`. These files establish same-day
  implementation and documentation activity, not completed scientific
  validation.
- The committed project chat summary and `AGENTS.md` establish the repository
  notes—not archived chats or automation-local memory—as the durable context
  for future project work.
- This documentation update did not modify programs or configuration, run
  tests, or run the scientific processing pipeline.

## Verification

- No tests were run during this log update. No dated test transcript was
  reviewed that verifies the current uncommitted worktree.
- The uncommitted project summary records a focused 45-test implementation
  checkpoint for the pre-P screening work, but its test transcript was not
  reviewed in this update; it is not treated as current-day verification here.
- No scientific processing was run during this log update, and no output file
  with a 2026-07-26 modification time was found.
- The most recent independently recorded full-suite result remains **40
  passing tests** from 2026-07-22. It is historical and does not validate the
  current uncommitted changes.

## Current active configuration

- Input is 250 Hz snippets, all channels (`all_channels: true`), S alignment,
  1--10 Hz filtering, and a -2--12 s analysis interval. The separate
  configured component is `T`.
- Event-static correction is enabled, using the shared catalog `time shift`
  column. The event reference is `CI_40353472`, and the event-stack alignment
  limit is 0.2 s.
- Station-static mode is now `cross_correlation`, using the configured
  external `stations.xlsx` path and `station static s` column. The correlation
  threshold is 0.4; the maximum station move is 0.05 s.
- The active trace-quality threshold is
  `trace_peak_to_pre_p_median_min: 20.0`. The current code/configuration
  change is not evidence that this threshold improves alignment or stack
  coherence.
- JSON event-location override remains enabled at 33.480 N, 116.513 W, and
  9.300 km. Individual-seismogram, original-overlay, and record-section
  plotting are disabled.

## Outputs and results reviewed

- No new scientific output was verified today.
- The tracked `CI_40353552` static-comparison workbook remains the latest
  reviewed report. Its embedded provenance identifies 2026-07-22 residual
  inputs, so it is not evidence of a 2026-07-26 run.

## Scientific observations and interpretation

- No new pipeline-derived scientific conclusion was established today.
- FS-003, FS-004, and FS-005 remain unchecked in `TASKS.md`. The present
  uncommitted implementation and configuration changes do not meet their
  stated multi-event/component/frequency validation requirements.

## Decisions

- Use the committed `AGENTS.md`, `README.md`, `Notes/`, and `TASKS.md` for
  durable FaultScan context; retain automation `memory.md` only for the local
  daily-log workflow.
- Keep same-day implementation state, recorded historical checkpoints, and
  newly rerun scientific evidence distinct.

## Unresolved questions and next steps

- FS-003: verify static-shift coding and application across representative
  events, components, frequencies, and station counts.
- FS-004: compare matched `none`, `tabulated`, and `cross_correlation`
  station-static runs and summarize alignment, correlation, and coherence.
- FS-005: establish and validate P-wave station shifts without overwriting the
  existing S-statics column.
- Run an appropriate test set and representative processing only when desired
  to validate the pending screening, stacker, and plotting changes.
