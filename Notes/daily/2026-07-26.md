# FaultScan Daily Log — 2026-07-26

## Work completed

- Reviewed the repository state, recent confirmed repository activity,
  `TASKS.md`, and the active `rp_input.json` configuration while preparing
  this log.
- No commits or branch reflog entries are dated today. The latest commit
  remains `4348661` (`Add static alignment modes and stacker workflow`,
  2026-07-25 19:31 PDT).
- No repository files or output files had a 2026-07-26 modification time
  before this documentation update. The current uncommitted working tree is
  unchanged from the prior log: `align_stack.py`, `align_utils.py`,
  `rp_input.json`, and `tests/test_align_utils_unit.py` contain the pending
  trace peak/pre-P median screening work.
- This documentation update did not modify programs or configuration,
  implement a task, run tests, or run the scientific processing pipeline.

## Verification

- No tests were run during this log update, and no same-day test transcript or
  result was found that verifies the current pending changes.
- No scientific processing was run during this log update, and no new
  pipeline output dated today was found.
- The most recent recorded full-suite result remains **40 passing tests** from
  2026-07-22. It is historical verification and does not validate the
  2026-07-25 commit or current uncommitted changes.

## Current active configuration

- Input is 250 Hz snippets, all channels (`all_channels: true`), S alignment,
  1--10 Hz filtering, and a -2--12 s analysis interval. The separate
  configured component is `T`.
- Event-static correction is enabled, using the shared catalog `time shift`
  column. The event reference is `CI_40353472`, and the event-stack alignment
  limit is 0.2 s.
- Station-static mode is `tabulated`, using `station static s` from the
  configured external `stations.xlsx` file. The correlation threshold is 0.6;
  the pending configuration also sets the trace peak/pre-P-median threshold to
  10.0.
- JSON event-location override remains enabled at 33.480 N, 116.513 W, and
  9.300 km. Individual-seismogram, original-overlay, and record-section
  plotting are disabled.

## Outputs and results reviewed

- No new scientific result was verified today.
- The tracked `CI_40353552` static-comparison workbook remains the latest
  reviewed report. Its embedded provenance identifies 2026-07-22 residual
  inputs, so it is not evidence of a new run today.

## Scientific observations and interpretation

- No new scientific conclusion was drawn today.
- FS-003, FS-004, and FS-005 remain unchecked in `TASKS.md`; the pending
  screening changes do not complete their required validation.

## Decisions

- Preserve the distinction between unchanged implementation state and new,
  verified processing evidence.

## Unresolved questions and next steps

- FS-003: verify static-shift coding and application across representative
  events, components, frequencies, and station counts.
- FS-004: compare matched `none`, `tabulated`, and `cross_correlation`
  station-static runs and summarize alignment, correlation, and coherence.
- FS-005: establish and validate P-wave station shifts without overwriting the
  existing S-statics column.
- Run an appropriate test set and representative processing only when desired
  to validate the pending screening changes.
