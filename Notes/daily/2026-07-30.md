# FaultScan Daily Log — 2026-07-30

## Work completed

- Reviewed the current repository state, confirmed commit activity,
  `TASKS.md`, and the active `rp_input.json` configuration while preparing
  this log.
- Confirmed three July 30 commits on `main`: `1354ca0` restricts aligned
  component-stack plots and their alignment calculations to catalog
  `skip = 0` events and adds smoke coverage; `f2dcad8` records P-phase stack
  comparison configuration cases; and `a3dce8c` records the late-July project
  activity, including this daily note's earlier version.
- At this review, `HEAD`, `main`, and `origin/main` are `a3dce8c` (`Record
  late July project activity`). The only uncommitted change before this log
  update was local `.DS_Store` metadata, which was preserved.
- These commits are implementation, configuration, and documentation state;
  they do not by themselves establish completed scientific tasks or results.
- This automation updated only this daily log. It did not modify programs,
  configuration, research inputs, or scientific outputs.

## Verification

- No tests were run during this log update. The committed project record
  reports a 78-test full discovery result on July 28 and a later 32-test
  focused smoke checkpoint for the then-local aligned-stack changes; neither
  was rerun by this automation. They are implementation verification, not
  representative scientific validation.
- No scientific processing was run, and no new scientific outputs or plots
  were generated or reviewed today.

## Current active configuration

- Input is 250 Hz snippets with all channels enabled, P alignment, 10--25 Hz
  filtering, and a 2--8 s analysis interval. The separate configured component
  is `T`; the active event list contains 27 events.
- Event-static correction is enabled, applying catalog `time shift` values.
  `CI_40353472` is the event-alignment reference, with a 0.2 s maximum
  event-stack alignment shift.
- Station-static mode is `none`; the configured external station workbook and
  `station static s` column are retained but not applied. `r_window_min` is
  -1, maximum station move is 0.05 s, and the pre-P noise-screen threshold is
  `trace_peak_to_pre_p_median_min: 3.0`.
- JSON event-location override is enabled at 33.480 N, 116.513 W, and 9.300
  km. Individual seismograms, original overlays, and record-section plotting
  are disabled.

## Scientific observations and interpretation

- No new pipeline-derived scientific conclusion was established today.
- FS-003, FS-004, and FS-005 remain unchecked in `TASKS.md`. Their stated
  representative validation criteria have not been demonstrated by this
  repository review.

## Unresolved questions and next steps

- Run an appropriate test set before relying on the July 30 implementation
  and configuration changes beyond their recorded focused smoke coverage.
- Validate FS-003 and FS-004 with matched `none`, `tabulated`, and
  `cross_correlation` runs before interpreting station-static effects.
- Validate P-wave statics for FS-005 without overwriting the established
  S-static column.
