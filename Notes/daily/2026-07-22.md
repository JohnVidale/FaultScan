# FaultScan Daily Log — 2026-07-22

## Work completed

- Consolidated the project's technical history, current processing status,
  scientific goals, repository structure, and open questions into durable
  Markdown notes.
- Added commit `fdf9001` (`Add alignment tools and project status notes`) to
  `main`; local `main` and `origin/main` currently point to this commit.
- Reviewed the earlier recovery of the July 13 working pipeline and retained
  the two July 18 safety stashes. They should not be deleted until the
  recovered and superseded states are no longer needed.
- Integrated cross-event component-stack alignment using reference event
  `CI_40353472`, including aligned offset-stack plots and Excel workbooks with
  residual shifts, correlations, reference flags, and catalog-shift metadata.
- Replaced the ambiguous event-stack control with
  `use_event_static_correction`: when true, the pipeline applies catalog event
  statics; when false, it leaves catalog origins unshifted and measures
  waveform residuals between event stacks.
- Corrected static-workbook provenance so exports record the actual catalog
  shift column used. The active processing column is the shared `time shift`
  column for Z, R, and T.
- Reworked catalog updating into
  `tools/update_catalog_time_shifts.py`. The JavaScript entry point now wraps
  the Python implementation. The updater validates that cross-correlation
  residuals were measured and combines them with the catalog baseline recorded
  by the run instead of blindly replacing prior corrections.
- Added or expanded tests for event-static behavior, static-workbook
  provenance, catalog updating, pipeline branches, and temporary output paths.
- Created a repository `README.md`, `Notes/faultscan_science_goals.md`, and
  updated project status and chat-summary notes.
- Established a project-memory workflow: objective processing records should
  be generated automatically, while scientific observations, hypotheses, and
  reasons for decisions should be preserved in Markdown daily/topic notes.

## Verification

- Full test discovery in the `vidale_main` Conda environment passed on the
  current working tree: **40 tests passed**.
- This test run used temporary directories and did not create a new research
  data-processing output directory.

## Current active configuration

- Input: 250 Hz snippets, all three components, S alignment, 3--10 Hz filter.
- Analysis interval: 0--12 s; correlation window padding is 0.05 s before and
  0.15 s after; maximum station move is 0.05 s.
- JSON event location override is enabled at 33.480 N, 116.513 W, 9.3 km.
- Catalog event-static correction is enabled using the shared `time shift`
  column; cross-event residual correlation is therefore disabled for this run
  mode.
- Station-static correction is enabled using `station static s` in
  `stations.xlsx`.
- Individual seismograms, original-trace overlays, and record-section plots are
  enabled.
- `all_channels: true` means the separate `component: T` value does not limit
  processing to T.

## Outputs and results reviewed

- Existing R and T all-event alignment products were identified in recent
  research output directories. Each pair consists of
  `all_events_<component>_stack_xcorr_alignment_to_CI_40353472.xlsx` and
  `all_events_<component>_offset_stacks_S_xcorr_aligned_to_CI_40353472.png`.
- Six station-static cases were reviewed: `Z_P`, `Z_S`, `R_P`, `R_S`, `T_P`,
  and `T_S`, with 289 stations in each analyzed case.
- The session-level six-case workbook contains long-form results, case
  summaries, median statics, uncertainties, and outlier counts.
- The R-S versus T-S comparison used 289 common stations. Correlation was about
  0.008, median absolute difference about 0.0127 s, and maximum absolute
  difference about 0.1216 s. The two estimates should not yet be treated as
  interchangeable station corrections.
- `statics_results_explanation.html` was confirmed to be a static explanatory
  snapshot rather than the output of a tracked reproducible generator.

## Scientific observations and interpretation

- The main scientific objective is to detect time-dependent structure near the
  S wave across the M3.4 mainshock while separating temporal changes from
  hypocentral offsets, origin-time errors, source duration, magnitude, station
  response, and processing artifacts.
- Station-quality observations were preserved for the excluded nodes: 50 bad
  T; 58 noisy; 74 and 85 small R; 107 and 331 bad R; 154, 161, and 303 bad T;
  171 and 179 both horizontal components dead.
- These labels do not establish whether the cause is instrumental or local
  site response.
- Event `CI_40353408` is the M3.41 mainshock. Its 297-degree nodal plane is
  approximately parallel to the local San Jacinto fault trace and is the
  favored right-lateral interpretation, although the conjugate plane remains
  possible.

## Decisions

- Continue applying the shared catalog `time shift` correction to all waveform
  components. Preserve `time shift T` as a diagnostic comparison until its
  scientific role is decided.
- Treat station and event corrections as enabling measurements, not as final
  evidence of a fault-zone change.
- Use the `vidale_main` environment for the pipeline and tests because the
  default Python environment lacks ObsPy.
- Keep Codex as the engineering environment and VS Code as the execution,
  debugging, Git, and visualization environment.
- Maintain project memory in real files under `Notes/`; AI conversations are
  useful for analysis but are not the permanent notebook.

## Unresolved questions and next steps

- Determine why R-S and T-S station statics disagree: possible causes include
  orientation, site response, phase selection, signal quality, and
  event-dependent effects.
- Decide whether T-derived event shifts remain diagnostic, replace the shared
  R-derived shifts, or are combined with R using a justified method.
- Determine whether rejected-station behavior reflects instrument problems,
  site effects, or both.
- Run a representative full data analysis with the current configuration and
  inspect its waveform figures and workbook provenance; unit tests alone do
  not validate the scientific result.
- Rerun each selected component/phase case before regenerating the six-case
  workbook, comparison plots, and HTML explanation so all products share the
  same inputs.
- Decide whether to add an explicit before/after-alignment waveform plot and a
  reproducible generator for the HTML report.
- Review the working tree before committing. It contains source,
  configuration, test, tool, README, and notes changes; `.DS_Store` should not
  be committed. Four PNG assets referenced by the static HTML report currently
  appear deleted and should either be restored or the report should be updated
  deliberately.
- Reconcile documentation with the active configuration: the current status
  note still describes station-static correction and individual seismogram
  plotting differently from `rp_input.json`.
- Decide whether daily notes alone are sufficient or whether enduring topics
  should also be promoted into files such as `station_quality.md` and
  `paper_ideas.md`.
