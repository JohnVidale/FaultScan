# FaultScan Daily Log — 2026-08-01

## Work completed

- Reviewed the current repository state, confirmed commit and reflog history,
  `TASKS.md`, and the active `rp_input.json` configuration while preparing
  this log.
- No commits dated August 1 were present in the inspected local history. The
  current `HEAD` is `f846e51` (`Record July project updates`), following the
  July 31 commits `3ba5c70` (S-phase cross-correlation inspection), `23cad9e`
  (obsolete configuration cleanup), and `f846e51` (project-record
  reconciliation).
- The working tree contains uncommitted changes to `README.md`,
  `align_stack.py`, `rp_input.json`, and `tests/test_align_stack_smoke.py`,
  plus local `.DS_Store` metadata and the untracked July 31 daily log. File
  timestamps place the four source/configuration/test changes on August 1;
  they remain uncommitted and were preserved.
- The pending implementation changes describe phase-aware tabulated station
  statics, retain cross-correlation residual measurement for either phase,
  accept the legacy `tabulate_T` mode spelling, document static-column
  selection, and add focused smoke-test cases. This is repository state, not
  evidence that the behavior has been tested in this checkout or used in a
  scientific run.
- This automation created only this daily log. It did not modify programs,
  configuration, research inputs, or scientific outputs.

## Verification

- No tests were run during this log update. The new pending smoke-test cases
  are present but unexecuted; the historical 23-test `align_stack` smoke
  checkpoint recorded in the July 31 project summary was not rerun.
- No scientific processing was run, and no new scientific outputs or plots
  were generated or reviewed today.

## Current active configuration

- `rp_input.json` selects 250 Hz snippets, all channels, T/S alignment, a
  10--25 Hz filter, and a 4.5--6.5 s interval. The configured event list
  contains 27 events, and `CI_40353472` remains the event-alignment reference
  with a 0.2 s maximum event-stack shift.
- Station-static mode is currently `tabulated`, using the external
  `stations.xlsx` workbook and its `sta_statics_T` column. This is an
  uncommitted configuration setting and is not evidence that a run occurred.
  The current screening values include `r_window_min: 0.5`, a 0.05 s move
  limit, and a 10.0 peak-to-pre-P-median threshold.
- JSON event-location override is enabled at 33.480 N, 116.513 W, and
  9.300 km. Individual seismograms, original overlays, and record-section
  plotting are enabled.

## Scientific observations and interpretation

- No new pipeline-derived scientific conclusion was established today.
- FS-003, FS-004, and FS-005 remain open in `TASKS.md`; their stated
  representative validation criteria are not demonstrated by this repository
  review. In particular, the pending tabulated 1--4 Hz configuration named in
  FS-004 is not a matched comparison.

## Unresolved questions and next steps

- Review and test the pending implementation/configuration changes before
  relying on them; testing would verify implementation only, not scientific
  validation.
- Validate FS-003 and FS-004 with their stated representative matched
  component/frequency comparisons before interpreting station-static effects.
- Validate P-wave statics for FS-005 without overwriting the established
  S-static column.
