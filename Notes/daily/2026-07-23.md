# FaultScan Daily Log — 2026-07-23

## Work completed

- Created the untracked top-level `TASKS.md` and organized it as a curated
  queue with `Now`, `Next`, `Waiting`, `Ideas`, and `Closed` sections.
- Added FS-003 through FS-005 to `Now`; they remain unchecked. Their stated
  work is validation of event and station statics and, later, P-wave station
  shifts—not completed work.
- Added FS-006 and FS-007 to `Next`, FS-010 to `Waiting`, and FS-011 through
  FS-013 to `Ideas`. The completed FS-002 entry records its own 2026-07-22
  completion and verification.
- Updated the untracked `README.md` today; it lists `TASKS.md` as the curated
  work queue.
- No commits, branch reflog entries, or tracked source/configuration edits are
  dated today. `main` and `origin/main` remain at `fdf9001` (`Add alignment
  tools and project status notes`, 2026-07-22).
- This log update did not modify programs or configuration, implement a task,
  or run scientific processing.

## Verification

- No tests were run today.
- No alignment, waveform-processing, or other scientific pipeline run was
  performed today.
- The most recent recorded full-suite result is **40 passing tests** from the
  2026-07-22 daily log. This is historical verification, not a result from
  today or confirmation of the current uncommitted working tree.

## Current active configuration

- `rp_input.json` selects 250 Hz snippet input, all channels
  (`all_channels: true`), S alignment, 3--10 Hz filtering, and a 0--12 s
  analysis interval. The separate configured component is `T`.
- The event reference is `CI_40353472`; its stack-alignment limit is 0.2 s.
  The waveform correlation window padding is 0.05 s before and 0.15 s after,
  with a 0.05 s station-move limit.
- Event-static correction is enabled. The current source code applies the
  shared catalog `time shift` column when that flag is enabled; it does not
  measure event-stack cross-correlation residual shifts in that mode.
- Station-static correction is enabled, using `station static s` from the
  configured external `stations.xlsx` path. In this mode the code does not
  write newly measured station statics.
- JSON event-location override is enabled at 33.480 N, 116.513 W, and 9.300
  km. Individual-seismogram and record-section plotting are enabled.
- `overlay_original_on_individual` is present in the JSON, but no matching use
  of that key was found in `align_stack.py`.

## Outputs and results reviewed

- No research outputs were generated today.
- The working tree contains four deleted tracked station-statics PNG assets:
  the R-S and T-S plots, plus their event-baseline-corrected variants. Their
  intended disposition has not been established.
- Existing uncommitted changes are present in code, configuration, tests,
  notes, tools, and assets. They were reviewed as repository state only and
  were not changed by this log update.

## Scientific observations and interpretation

- No new scientific conclusion was reached today.
- The active task order correctly keeps validation of stored event and station
  shifts ahead of the planned foreshock/aftershock waveform-difference work.

## Decisions

- Use `TASKS.md` as the short, actively curated queue; do not treat an entry
  as complete unless its stated completion criteria and verification are
  recorded.
- Preserve the historical test result separately from today’s unrun tests and
  processing.

## Unresolved questions and next steps

- FS-003: verify that static shifts are coded and applied correctly using
  representative event, component, frequency, and station-count checks.
- FS-004: compare matched runs with and without stored S station statics.
- FS-005: select and validate P-wave calibration events before adding a new
  P-static station-table column.
- FS-006 and FS-007 remain planned waveform-comparison and explanation work;
  FS-010 awaits a reviewed relative-relocation catalog.
- Decide whether the four deleted station-statics image assets should be
  restored, regenerated, or deliberately removed, and whether the unused
  `overlay_original_on_individual` configuration key should be implemented or
  removed.
