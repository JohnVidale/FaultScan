# FaultScan Daily Log — 2026-07-28

## Work completed

- Reviewed the current repository state, confirmed commit and reflog activity,
  `TASKS.md`, and the active `rp_input.json` configuration while preparing
  this log.
- No commit or reflog entry dated 2026-07-28 was present at review time. The
  latest confirmed repository activity remains the July 27 commit series
  ending at `12f2adc` (`Document shared stack output workflow`).
- The current worktree contains uncommitted changes to alignment, snippet
  plotting, stack comparison, README, task, configuration, and test files;
  it also has three deleted reserve configuration files and local `.DS_Store`
  metadata. These are implementation and documentation state, not evidence of
  completed tasks or scientific results.
- This update created only this daily log. It did not modify programs,
  configuration, research inputs, or scientific outputs.

## Verification

- No tests were run during this update. Earlier daily notes and the project
  summary record historical focused and full-discovery test checkpoints, but
  they were not rerun today and do not validate the present uncommitted
  worktree.
- No scientific processing was run, and no new scientific outputs or plots
  were generated or reviewed today.

## Current active configuration

- Input is 250 Hz snippets with all channels enabled, P alignment, 1--4 Hz
  filtering, and a -2--12 s analysis interval. The separate configured
  component is `T`.
- Event-static correction is disabled, so catalog `time shift` values are not
  applied. `CI_40353472` remains the event-alignment reference, with a 0.2 s
  maximum event-stack alignment shift.
- Station-static mode is `none`; the configured station workbook and
  `station static s` column are retained but not applied. The correlation
  threshold is 0.2, maximum station move is 0.05 s, and the pre-P noise-screen
  threshold is `trace_peak_to_pre_p_median_min: 5.0`.
- JSON event-location override is enabled at 33.480 N, 116.513 W, and 9.300
  km. Individual seismograms, original overlays, and record-section plotting
  are enabled.

## Scientific observations and interpretation

- No new pipeline-derived scientific conclusion was established today.
- FS-003, FS-004, and FS-005 remain unchecked in `TASKS.md`. Their stated
  validation criteria have not been demonstrated by this repository review.

## Unresolved questions and next steps

- Run an appropriate test set before relying on the current uncommitted
  implementation and configuration changes.
- Validate FS-003 and FS-004 with matched `none`, `tabulated`, and
  `cross_correlation` runs before interpreting station-static effects.
- Validate P-wave statics for FS-005 without overwriting the established
  S-static column.
