# FaultScan Daily Log — 2026-07-28

## Work completed

- Reviewed the current repository state, the confirmed July 28 commit history,
  `TASKS.md`, the active `rp_input.json` configuration, and project status
  while preparing this log.
- Confirmed three commits on `main` between 18:59 and 19:00 PDT:
  `d8252b6` updated final-selection coloring in component diagnostics and its
  smoke test; `9c0b375` aligned plotting and stack tools, documentation,
  tests, and the active configuration, and removed three reserve configuration
  files; and `c3c20b0` refreshed the July daily records and project-chat
  summary.
- At review time, `HEAD`, `main`, and `origin/main` were all `c3c20b0`
  (`Refresh July project records`). The only uncommitted change was local
  `.DS_Store` metadata. The source, configuration, task, test, and record
  changes described above are committed.
- This automation updated only this daily log. It did not modify programs,
  configuration, research inputs, or scientific outputs.

## Verification

- No tests were run during this log update. The July 28 commits include test
  changes, but no current-day test-execution transcript was reviewed; they do
  not establish validation of the committed implementation.
- No scientific processing was run, and no new scientific outputs or plots
  were generated or reviewed during this update.

## Current active configuration

- Input is 250 Hz snippets with all channels enabled, P alignment, 1--4 Hz
  filtering, and a -2--12 s analysis interval. The separate configured
  component is `T`.
- Event-static correction is disabled, so catalog `time shift` values are not
  applied. `CI_40353472` is the event-alignment reference, with a 0.2 s
  maximum event-stack alignment shift.
- Station-static mode is `none`; the configured external station workbook and
  `station static s` column are retained but not applied. The correlation
  threshold is 0.2, maximum station move is 0.05 s, and the pre-P noise-screen
  threshold is `trace_peak_to_pre_p_median_min: 5.0`.
- JSON event-location override is enabled at 33.480 N, 116.513 W, and 9.300
  km. Individual seismograms, original overlays, and record-section plotting
  are enabled.

## Scientific observations and interpretation

- No new pipeline-derived scientific conclusion was established today.
- FS-003, FS-004, and FS-005 remain unchecked in `TASKS.md`. Their stated
  representative validation criteria have not been demonstrated by this
  repository review.

## Unresolved questions and next steps

- Run an appropriate test set before relying on the July 28 implementation
  changes.
- Validate FS-003 and FS-004 with matched `none`, `tabulated`, and
  `cross_correlation` runs before interpreting station-static effects.
- Validate P-wave statics for FS-005 without overwriting the established
  S-static column.
