# FaultScan Daily Log — 2026-07-31

## Work completed

- Reviewed the current repository state, recent confirmed commit activity,
  `TASKS.md`, and the active `rp_input.json` configuration while preparing
  this log.
- Confirmed three July 31 commits on `main`: `3ba5c70` configures an S-phase
  cross-correlation inspection and updates the R/T snippet plotter;
  `23cad9e` removes the obsolete `rp_input 0.json` and `rp_input 0&1.json`
  variants; and `f846e51` reconciles the July 30 daily note and project chat
  summary.
- At this review, `HEAD`, `main`, and `origin/main` are `f846e51` (`Record
  July project updates`). The only uncommitted change before this log update
  was local `.DS_Store` metadata, which was preserved.
- These commits record implementation, configuration, cleanup, and
  documentation state. They do not by themselves establish completed
  scientific tasks or results.
- This automation updated only this daily log. It did not modify programs,
  configuration, research inputs, or scientific outputs.

## Verification

- No tests were run during this log update. The committed project chat summary
  records a previous 23-test `align_stack` smoke checkpoint; it was not rerun
  by this automation and is implementation verification, not representative
  scientific validation.
- No scientific processing was run, and no new scientific outputs or plots
  were generated or reviewed today.

## Current active configuration

- `rp_input.json` selects 250 Hz snippets, all channels, T/S alignment, a
  10--25 Hz filter, and a 4.5--6.5 s interval. The configured event list
  contains 27 events, and `CI_40353472` remains the event-alignment reference
  with a 0.2 s maximum event-stack shift.
- Station-static mode is `cross_correlation`; the retained external workbook
  and `station static s` column identify its input location/column but are not
  evidence that a run occurred. The current screening values include
  `r_window_min: 0.5`, a 0.05 s move limit, and a 10.0
  peak-to-pre-P-median threshold.
- JSON event-location override is enabled at 33.480 N, 116.513 W, and
  9.300 km. Individual seismograms, original overlays, and record-section
  plotting are enabled.

## Scientific observations and interpretation

- No new pipeline-derived scientific conclusion was established today.
- FS-003, FS-004, and FS-005 remain open in `TASKS.md`; their stated
  representative validation criteria are not demonstrated by this repository
  review.

## Unresolved questions and next steps

- Run an appropriate test set before relying on the July 31 implementation and
  configuration changes beyond the recorded historical smoke checkpoint.
- Validate FS-003 and FS-004 with their stated representative matched
  component/frequency comparisons before interpreting station-static effects.
- Validate P-wave statics for FS-005 without overwriting the established
  S-static column.
